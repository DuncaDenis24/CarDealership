package com.example.demo.config;

/*
 This file left intentionally simple. The main CORS configuration is provided
 by the CorsConfigurationSource bean in SecurityConfig.java above.
*/
public class CorsConfig {
    // kept for compatibility; no implementation needed because SecurityConfig defines CORS.
}
